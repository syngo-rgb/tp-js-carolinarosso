// Obtener la suma de los elementos de un array.
const array = [5, 10, 15, 20];
let suma = 0;
// Iterar sobre el array y sumar cada elemento
for (let i = 0; i < array.length; i++) {
    suma += array[i];
}
console.log("La suma de los elementos del array es: " + suma);
